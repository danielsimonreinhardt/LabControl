pyserial>=3.5
PySide6>=6.6
qtawesome>=1.3
asammdf>=8.0
# python-can>=4.5 statt >=4.3: slcan-Interface (can_bus/driver.py) nutzt den
# Konstruktor-Parameter `tty_baudrate`, der erst seit 4.5.0 unter diesem
# Namen existiert (davor `ttyBaudrate`).
python-can>=4.5
cantools>=40.0
picosdk>=1.1

# nur zum Bauen der .exe (siehe lab_gui/README.md)
pyinstaller>=6.0
